{
  "name": "bestchange-parser",
  "version": "0.1.0",
  "description": "exchange platforms parser",
  "private": true,
  "scripts": {
    "start:list": "node dist/apps/list/main.js",
    "start:contacts": "node dist/apps/contacts/main.js",
    "start:exporter": "node dist/apps/exporter/main.js",
    "dev:list": "npm run build && npm run start:list",
    "dev:contacts": "npm run build && npm run start:contacts",
    "dev:exporter": "npm run build && npm run start:exporter",
    "build": "tsc",
    "lint": "eslint . --ext .ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.12.0",
    "@typescript-eslint/parser": "^6.12.0",
    "eslint": "^8.54.0",
    "typescript": "~5.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "google-auth-library": "^9.2.0",
    "google-spreadsheet": "^4.1.1",
    "puppeteer": "^21.5.2",
    "ts-retry": "^4.2.4"
  }
}
